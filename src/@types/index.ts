export type PropsProject = {
  project: {
    id: string
    name: string
    description: string
    image: string
    stack: string
    tecnologies: string[]
    github: string
    page: string
  }
}
